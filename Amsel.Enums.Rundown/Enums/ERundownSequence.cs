namespace Amsel.Enums.Rundown.Enums
{
    public enum ERundownSequence
    {
        TRIGGER = 5,
        STRUCTURE = 10,
        LOAD = 15,

        TRANSITION_IN = 25,
        WHILE = 50,
        WAIT = 65,
        TRANSITION_OUT = 75,

        STOP = 90,

        CLEANUP = 100
    }
}